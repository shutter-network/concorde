A message arrived for you from user {{userId}}. They said:

{{text}}

Answer them, and answer them by **sending a Message**: `POST /messages` on the Gateway's Agent
server, with a body of `{"userId": "{{userId}}", "text": "your answer"}`. Your system prompt
says where that server is.

That call is the only thing in this deployment that reaches a person. Your final reply here is
read by nobody, and neither is anything you write into the Workspace.

One call is one person. This message is {{userId}}'s and your answer goes to {{userId}} and to
no one else.

It travels as an encrypted direct message over Nostr. The relay carries it and cannot read it,
and it reaches {{userId}} in whatever client they run rather than in a browser tab. So write
plain sentences, keep it to a couple of them, and do not use headings, tables or code blocks.
