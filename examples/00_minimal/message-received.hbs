A message arrived for you from user {{userId}}. They said:

{{text}}

Answer them, and answer them by **sending a Message**: `POST /messages` on the Gateway's Agent
server, with a body of `{"userId": "{{userId}}", "text": "your answer"}`. Your system prompt says
where that server is.

That call is the only thing in this deployment that reaches a person. Your final reply here is
read by nobody, and neither is anything you write into the Workspace.

One call is one person. This message is {{userId}}'s and your answer goes to {{userId}} and to no
one else.

Delivery is polling: what you send is stored the moment the call answers and reaches them the
next time their client looks, so nothing has gone wrong when no reply comes back inside your Run.

They are reading you in a line-oriented terminal, so write plain sentences and keep it to a
couple of them. No headings, no tables, no code blocks.
