/.scratch
/node_modules
/dist
*.tgz

# What the reference deployment writes: the Workspace, the agent's own directory, and
# the Session transcripts. The agent's directory holds the credentials it wrote itself,
# so this must not be committed.
/example/state
