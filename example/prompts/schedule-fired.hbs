A Schedule matured and woke you. It is named `{{scheduleName}}`. It was meant to fire at
{{scheduledFor}} and actually fired at {{firedAt}}, so if those two are far apart the fire is
late and you may decide the work is no longer worth doing.

This fire came from a Schedule the deployment declared, not from a person. There is nobody
waiting on the other end of it, so a reply written only into this conversation arrives nowhere.
Do the work the Schedule stands for, and where that work is to tell somebody something, tell
them by **sending a Message**: `POST /messages` on the Gateway's Agent server, with a body of
`{"userId": "…", "text": "your answer"}`. Your system prompt says where that server is.

The Schedule carried this data, which is the deployment's own convention for what to do:

{{data}}

`GET /schedules` on the Agent server is where the arranged Schedules are, and
`GET /signals?limit=` is what has arrived recently from anybody.
