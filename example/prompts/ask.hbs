A message arrived for you from user {{user}}. They said:

{{text}}

Before you answer, read the Gateway's Agent server to see what else has arrived
recently — your system prompt says where it is and how to reach it.

Then append your answer to `answers.md` in the Workspace, which is your working
directory. The Gateway's Signal Handlers share it with you, and a Handler wrote this
out of a Signal that arrived there. Nothing in this deployment delivers your answer to
anyone — there is no Messenger yet — so the file is where it lands.

Keep it to a couple of sentences.
