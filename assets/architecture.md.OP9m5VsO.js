import{_ as s,o as t,c as a,a0 as n}from"./chunks/framework.Czcffv53.js";const g=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md"}'),i={name:"architecture.md"};function r(o,e,h,l,d,p){return t(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><p>This page explains what the parts are and why they are separate. The <a href="./guide.html">guide</a> builds a deployment step by step. The <a href="./reference/">API reference</a> gives exact signatures.</p><h2 id="the-shape" tabindex="-1">The shape <a class="header-anchor" href="#the-shape" aria-label="Permalink to &quot;The shape&quot;">​</a></h2><p>A Shared Agent is one deployable application, assembled from parts. This is the path that one message takes through it:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   a person                                time</span></span>
<span class="line"><span>      |                                     |</span></span>
<span class="line"><span>      v                                     |</span></span>
<span class="line"><span>   Channel          one medium              |</span></span>
<span class="line"><span>      |                                     |</span></span>
<span class="line"><span>      v                                     v</span></span>
<span class="line"><span>   Messenger  --&gt; Message log            Scheduler</span></span>
<span class="line"><span>      |                                     |</span></span>
<span class="line"><span>      |  emits a Signal                     |  emits a Signal</span></span>
<span class="line"><span>      +------------------+------------------+</span></span>
<span class="line"><span>                         |</span></span>
<span class="line"><span>                         v</span></span>
<span class="line"><span>                  Signal Worker            one Run at a time, globally</span></span>
<span class="line"><span>                         |</span></span>
<span class="line"><span>                         v</span></span>
<span class="line"><span>                  Signal Handler           your code</span></span>
<span class="line"><span>                         |</span></span>
<span class="line"><span>                         |  zero or more Prompts</span></span>
<span class="line"><span>                         v</span></span>
<span class="line"><span>                      Runtime</span></span>
<span class="line"><span>                         |</span></span>
<span class="line"><span>                         v</span></span>
<span class="line"><span>              Agent Implementation         in a container</span></span>
<span class="line"><span>                         |</span></span>
<span class="line"><span>                         |  calls back over HTTP</span></span>
<span class="line"><span>                         v</span></span>
<span class="line"><span>                   Agent server            no authentication</span></span></code></pre></div><p>Those parts sit in three rings, from the inside out:</p><ol><li><strong>The Agent Implementation</strong> runs the model. It is <code>pi</code> by default, driven by a Runtime, and it runs in a container.</li><li><strong>The Signal Worker</strong> owns the Signal queue, the dispatch to Handlers, and the Runs. It holds no identity and knows nothing about messaging.</li><li><strong>Producers</strong> are trusted parts that emit Signals into the Worker. The Messenger and the Scheduler are the two that ship.</li></ol><p>Only the Gateway connects a person to the agent. There is no other path.</p><h2 id="the-loop" tabindex="-1">The loop <a class="header-anchor" href="#the-loop" aria-label="Permalink to &quot;The loop&quot;">​</a></h2><p>One message produces one pass through this loop:</p><ol><li>A person sends a Message to a <strong>Channel</strong>.</li><li>The Channel hands it to the <strong>Messenger</strong>, which stores it and emits a <strong>Signal</strong>. Both happen in one transaction.</li><li>The <strong>Signal Worker</strong> takes the oldest pending Signal.</li><li>It dispatches on the Signal&#39;s <code>kind</code> to exactly one <strong>Signal Handler</strong>.</li><li>The Handler returns zero or more <strong>Prompts</strong>, each naming a <strong>Session</strong>.</li><li>For each Prompt the <strong>Runtime</strong> starts a <strong>Run</strong>.</li><li>During the Run the agent calls the Agent server. It reads Messages, sends Messages, and reads Users.</li><li>An outbound Message is written to the log and handed to the Channel.</li><li>The Handler&#39;s optional <strong>post phase</strong> runs, and is told whether any Run failed.</li></ol><p>The person reads the answer by polling their own Message log, or receives it in their own client, depending on the Channel.</p><h2 id="the-gateway" tabindex="-1">The Gateway <a class="header-anchor" href="#the-gateway" aria-label="Permalink to &quot;The Gateway&quot;">​</a></h2><p>A <strong>Gateway</strong> is a record of Components, keyed by names you choose. It starts them in key order and stops them in the reverse of that order. A failed start unwinds what already started.</p><p>A <strong>Component</strong> is a <code>start</code> and a <code>stop</code>, and nothing else. It has no name, declares no dependency, and resolves nothing. The parts already hold each other, because you passed them to each other.</p><p>This is not a plugin system and not a registry. A part handed a server registers its routes in its own constructor.</p><h3 id="creategateway" tabindex="-1">createGateway <a class="header-anchor" href="#creategateway" aria-label="Permalink to &quot;createGateway&quot;">​</a></h3><p><code>createGateway</code> builds the irreducible infrastructure that every deployment needs:</p><table tabindex="0"><thead><tr><th>Key</th><th>What it is</th></tr></thead><tbody><tr><td><code>db</code></td><td>The PostgreSQL client. It owns the pool and the <code>LISTEN</code> connections.</td></tr><tr><td><code>agentServer</code></td><td>The server the Agent Implementation reaches.</td></tr><tr><td><code>publicServer</code></td><td>The server people reach.</td></tr><tr><td><code>worker</code></td><td>The serial Signal Worker.</td></tr></tbody></table><p>Your <code>extend</code> function receives those four and returns components of your own. Your <code>handlers</code> function runs after <code>extend</code>, so a Handler can reach a component you built. The reverse is not possible.</p><p>The four infrastructure keys are a type error in what <code>extend</code> returns. Otherwise a spread overwrites one in silence.</p><p><code>createBareGateway</code> takes a record of Components and is the escape one layer down. A deployment whose infrastructure shape itself differs uses it.</p><h3 id="start-order" tabindex="-1">Start order <a class="header-anchor" href="#start-order" aria-label="Permalink to &quot;Start order&quot;">​</a></h3><p>The Signal Worker is keyed last, so it stops first. Its <code>stop</code> is the only one that <strong>waits on work in flight</strong>: it waits for the Run. That drain therefore runs while every server still listens and the pool is still open.</p><p>Other stops release resources rather than wait. The Db closes its pool and its <code>LISTEN</code> connections, each server closes itself, and the Nostr Channel closes its Relay connection.</p><p>Your own components are keyed ahead of the Worker. They start before it and stop after the drain. That is correct for anything the drain uses, such as the Messenger a post phase calls.</p><h2 id="the-two-servers" tabindex="-1">The two servers <a class="header-anchor" href="#the-two-servers" aria-label="Permalink to &quot;The two servers&quot;">​</a></h2><p>The difference between the two servers is the trust boundary.</p><table tabindex="0"><thead><tr><th></th><th>Public server</th><th>Agent server</th></tr></thead><tbody><tr><td>Who reaches it</td><td>People</td><td>The Agent Implementation</td></tr><tr><td>Authentication</td><td>Every registered Auth</td><td><strong>None</strong></td></tr><tr><td>Sees Signals</td><td>No</td><td>Yes</td></tr><tr><td>Reads any person&#39;s Message log</td><td>No</td><td>Yes</td></tr><tr><td>Can sign a Statement</td><td>No</td><td>Yes</td></tr><tr><td>Can create a User</td><td>No</td><td><strong>No</strong></td></tr><tr><td>Can set another person&#39;s password</td><td>No</td><td>No</td></tr></tbody></table><p>Neither server creates a User, and neither sets a password for somebody else. Both are methods that only your own code can call.</p><p>Reaching the Agent server port is access to every route on it. Keeping that port unreachable is the deployment&#39;s responsibility.</p><p>A person reaches Users, their Auth&#39;s own routes, a Channel, Decisions, and two of the three Signature routes. Over HTTP with Password Auth, those Auth routes are a login, a logout, and a change of their <strong>own</strong> password. People never see a Signal, and they never reach <code>POST /sign</code>.</p><p>Both servers describe themselves. Each registers OpenAPI generation <strong>before</strong> <code>extend</code> runs, because route discovery is a hook that fires as each component registers. The document at <code>/openapi.json</code> is therefore the truth about what a Gateway serves.</p><h2 id="the-runtime-and-the-agent-implementation" tabindex="-1">The Runtime and the Agent Implementation <a class="header-anchor" href="#the-runtime-and-the-agent-implementation" aria-label="Permalink to &quot;The Runtime and the Agent Implementation&quot;">​</a></h2><p>The <strong>Runtime</strong> is the narrowest interface in the framework:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Runtime</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">prompt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RunPrompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RunOutcome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>One method, one argument, one outcome. A second Agent Implementation is one function.</p><p>The Runtime is held by the Signal Worker and is never started. It is not a Component.</p><p>It is called one Run at a time, never concurrently. No implementation needs locking.</p><p><code>createPiRuntime</code> runs <code>pi</code> in a container. The Prompt goes on standard input, never in the argument list. Nothing about the model, the provider, or the session directory comes from the framework: those come from a mounted settings file and from the image.</p><h3 id="the-agent-container" tabindex="-1">The Agent Container <a class="header-anchor" href="#the-agent-container" aria-label="Permalink to &quot;The Agent Container&quot;">​</a></h3><p>The container plumbing is separate from <code>pi</code>, on its own subpath, because a second Agent Implementation needs it unchanged. It is what <code>docker run</code> takes, and what to do with the result.</p><p>A <strong>Mount Table</strong> declares what the container reaches on disk. It has one required <code>runtimeDir</code>, which is a path on the <strong>host</strong>, and every entry is written relative to it. The table verifies nothing about the filesystem, and it refuses four things that cannot mean what they say:</p><ul><li>An <code>agentPath</code> that is not absolute.</li><li>A leading slash on an entry&#39;s <code>path</code>.</li><li>A <code>.</code> or <code>..</code> segment in a resolved path.</li><li>Two entries that resolve to one target.</li></ul><p>Every Run is <code>--rm</code> and <code>--interactive</code>, which holds stdin open and gives the container no TTY. It runs as the Gateway process&#39;s own user and group, where the platform reports them. Env values are redacted in the loggable copy of the command.</p><h2 id="signals-runs-and-handlers" tabindex="-1">Signals, Runs, and Handlers <a class="header-anchor" href="#signals-runs-and-handlers" aria-label="Permalink to &quot;Signals, Runs, and Handlers&quot;">​</a></h2><p>A <strong>Signal</strong> is an arrival record, emitted by a trusted Producer. It carries a <code>kind</code>, an arbitrary JSON <code>payload</code>, and a timestamp. There is <strong>no user id column</strong>: the Signal Worker authenticates nobody, so attribution is not a fact it holds. The Messenger&#39;s payload carries the User id, which is trustworthy because that component wrote it.</p><p>A <strong>Run</strong> is one Prompt executed in one Session. Its <code>session</code> is a plain name, not a foreign key. Sessions live inside the Agent Implementation.</p><p>A <strong>Signal Handler</strong> is arbitrary code and is the primary extension point:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SignalHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  handle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">signal</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Signal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]&gt;;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  post</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">signal</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Signal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">outcome</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PostOutcome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>handle</code> receives the Signal and nothing else. There is no context object and no second argument.</p><p>Returning zero Prompts means the Signal is done and no Run happens. The arrival record stays, so a refusal is auditable. Returning many Prompts fans out, and one failure does not stop the rest.</p><p>The <strong>post phase</strong> runs once, after every Run from that Signal has finished. It cannot produce Prompts. It is the whole of the framework&#39;s failure handling, and it is how &quot;that failed&quot; reaches the person who asked.</p><p><code>templateHandler</code> is the common case. It renders a Handlebars template into one Prompt. The template is source text, never a path, and it compiles when the Gateway is built. A template that does not compile therefore fails construction rather than a Signal.</p><h3 id="at-most-once" tabindex="-1">At-most-once <a class="header-anchor" href="#at-most-once" aria-label="Permalink to &quot;At-most-once&quot;">​</a></h3><p>A Signal that fails is never retried. A Signal left in <code>processing</code> by a stopped Worker is failed at the next start. Nothing cancels, retries, reprioritizes, or removes a Signal.</p><p>This is why the post phase exists, and why a template compiles early.</p><h2 id="core-components" tabindex="-1">Core components <a class="header-anchor" href="#core-components" aria-label="Permalink to &quot;Core components&quot;">​</a></h2><p>Every part a deployment assembles is a Component. A deployment builds only the ones it wants, and each one below is a single <code>create*</code> call inside <code>extend</code>.</p><h3 id="users" tabindex="-1">Users <a class="header-anchor" href="#users" aria-label="Permalink to &quot;Users&quot;">​</a></h3><p>Users owns who exists, and their attributes. It owns no credential.</p><p>Creating a User and setting attributes are <strong>methods, never routes</strong>. An agent that can create a User and give it a credential has made itself an account.</p><p>Nothing removes a User. Revoking their Tokens is the whole of it.</p><p>Users is not a Producer. A Signal for each login puts a Run behind every authentication, and the Worker is serial.</p><h3 id="auth" tabindex="-1">Auth <a class="header-anchor" href="#auth" aria-label="Permalink to &quot;Auth&quot;">​</a></h3><p>An <strong>Auth</strong> owns one scheme&#39;s secret, and turns a request carrying it into a User:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Auth</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Component</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> scheme</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  authenticate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FastifyRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AuthOutcome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>An Auth answers with a User record rather than an id, because the server that walks the Auths can resolve nothing itself.</p><p>The Public server aggregates them. It reads the registered Auths on each request, in registration order. The first <code>authenticated</code> outcome assigns the User and continues. The first <code>refused</code> outcome ends the request. Every 401 carries one challenge per registered <strong>Auth</strong>, so two Auths that carry one scheme name it twice.</p><p>Two Auths ship:</p><ul><li><strong>Password Auth</strong> owns scrypt hashes and Tokens. A password is traded for a bearer Token. The login derives against a fixed dummy digest when the User is unknown, so a miss costs what a hit costs.</li><li><strong>Nostr Auth</strong> verifies a NIP-98 event on every request and issues nothing. It records each admitted event, so a credential is spent once. Its freshness window applies in <strong>both</strong> directions, so an event stamped in the future is refused rather than valid forever.</li></ul><p>Both own tables. An Auth is the component most likely to be assumed stateless, and what an Auth owns is a secret. A secret is a row.</p><h3 id="the-messenger-and-channels" tabindex="-1">The Messenger and Channels <a class="header-anchor" href="#the-messenger-and-channels" aria-label="Permalink to &quot;The Messenger and Channels&quot;">​</a></h3><p>Messaging is two parts.</p><p><strong>The Messenger owns the Message log and reaches nobody.</strong> One table, one <code>text</code> per Message, and one sequence per User across both directions. One cursored read therefore serves both polling and rendering.</p><p><strong>A Channel is what reaches a person over one medium:</strong></p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Channel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Component</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TSchema</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    tx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Handle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    message</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MessageRecord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  )</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>send</code> receives an outbound Message inside the transaction that is writing it. The promise is only that the Message is the Channel&#39;s now. Arrival is not something every medium can promise.</p><p><strong>A Channel must not perform the network act inside <code>send</code>.</strong> A publish cannot be rolled back and a transaction can. An implementation does everything knowable synchronously, throws for anything wrong, and lets whatever must travel travel after the commit.</p><p>There is no public <code>receive</code>. Registration hands back a private handle, so no other code can put words in a person&#39;s log.</p><p>One Channel per Messenger is refused at registration. A deployment runs one medium.</p><p>Two Channels ship:</p><ul><li><strong>The HTTP Channel</strong> has no tables and no work at either end. Its <code>send</code> is a no-op, because HTTP delivery is the person asking. It serves a submission route and a cursored poll.</li><li><strong>The Nostr Channel</strong> exchanges NIP-17 private direct messages over one Relay. It owns three tables: which key is which User, which envelopes it has read, and which replies the Relay has not taken. It registers no route on either server.</li></ul><p>The Nostr Channel is the counter-example that says a Channel is not a tableless kind of thing.</p><h3 id="signatures-and-decisions" tabindex="-1">Signatures and Decisions <a class="header-anchor" href="#signatures-and-decisions" aria-label="Permalink to &quot;Signatures and Decisions&quot;">​</a></h3><p><strong>Signatures</strong> holds the agent&#39;s signing identity and stores nothing at all. What it makes is a <strong>Signed Statement</strong>: a compact JWS, verifiable by ordinary JOSE tooling in any language.</p><p>The deployment loads the key. The framework parses no PEM and generates no keypair. A deployment brings its own identity or does not start.</p><p><strong>Decisions</strong> is the log of commitments worth keeping. It is global, addressed to nobody, and read by any authenticated person. It is the framework&#39;s only shared read: everyone sees the same rows.</p><p>What a signature proves is narrow. It proves that the Operator committed to this Statement on the agent&#39;s behalf. It proves nothing whatever about the agent&#39;s conduct.</p><p>Its audience is a party who never touches the Gateway and holds only a public key.</p><h3 id="the-scheduler" tabindex="-1">The Scheduler <a class="header-anchor" href="#the-scheduler" aria-label="Permalink to &quot;The Scheduler&quot;">​</a></h3><p>The Scheduler emits one fixed kind when a Schedule matures. It is a timer in front of the ordinary dispatch, and it adds no Handler concept.</p><p>It supports recurrence, one-shots, cancellation, and time zones. A missed fire is never replayed: every next fire is derived strictly forward from now.</p><p>Its agent-facing routes are optional. An agent that can wake itself can fill the one serial Signal lane and never empty it.</p><h3 id="two-identities-held-apart" tabindex="-1">Two identities, held apart <a class="header-anchor" href="#two-identities-held-apart" aria-label="Permalink to &quot;Two identities, held apart&quot;">​</a></h3><p>A Shared Agent has two identities and no more.</p><table tabindex="0"><thead><tr><th></th><th>Signing identity</th><th>Nostr identity</th></tr></thead><tbody><tr><td>Key</td><td>Ed25519, or P-256, P-384, P-521</td><td>secp256k1</td></tr><tr><td>Lives on</td><td>Signatures</td><td>The Nostr Channel</td></tr><tr><td>Answers to</td><td>A third party who never touches the Gateway</td><td>The people talking to the agent</td></tr><tr><td>Copying it</td><td>Forges the agent&#39;s commitments</td><td>Impersonates the agent to them</td></tr></tbody></table><p>Neither can be the other. Nostr requires secp256k1, and Signatures refuses that curve. Nothing in the framework answers &quot;who is this agent&quot; across both, and nothing tries.</p><h2 id="data-ownership" tabindex="-1">Data ownership <a class="header-anchor" href="#data-ownership" aria-label="Permalink to &quot;Data ownership&quot;">​</a></h2><p>Each part owns one PostgreSQL schema, named for its subject rather than for the part. No table references another part&#39;s, with six exceptions that all point one way.</p><p>Those six are foreign keys onto <code>saf_users.users.id</code>. The Messenger declares one, the Nostr Channel declares two, Password Auth declares two, and Nostr Auth declares one. The Users component references nobody back.</p><p>Half of them belong to an Auth. That is what &quot;the seam is who owns the secret&quot; costs in the schema.</p><p><strong>The framework applies no DDL.</strong> It ships schema definitions on a <code>/schema</code> subpath below each component, and the Operator applies them with their own <code>drizzle-kit</code>. There is no migration tracking table and no registration order.</p><p>Two components own no tables: Signatures, because a Signed Statement is never kept, and the HTTP Channel, because the log is the Messenger&#39;s and HTTP delivery needs no queue.</p><h2 id="what-you-must-provide-yourself" tabindex="-1">What you must provide yourself <a class="header-anchor" href="#what-you-must-provide-yourself" aria-label="Permalink to &quot;What you must provide yourself&quot;">​</a></h2><p>Each of these is a deliberate decision, not an omission. Read the whole list before you deploy.</p><ul><li><strong>Confidentiality between parties.</strong> The agent reads everything and decides what to send to whom.</li><li><strong>Resistance to prompt injection.</strong> This risk is accepted. Guidance to Handler authors is the only mitigation.</li><li><strong>Confinement of the Agent Implementation.</strong> The deployment confines it.</li><li><strong>An unreachable Agent server.</strong> There is no authentication on it. The bind address your entry point states is the whole of the protection.</li><li><strong>Rate limiting.</strong> The login route is unthrottled and no lockout exists. Rate limiting belongs at your edge, where it survives a second Gateway process.</li><li><strong>Account recovery.</strong> There is no email, no reset flow, and no security questions. A forgotten password is trusted code setting a new one.</li><li><strong>Removal of a person.</strong> Nothing removes or deactivates a User.</li><li><strong>Key rotation, for either identity.</strong> No record carries a key identifier.</li><li><strong>Non-repudiation of the conversation.</strong> No party can prove what the agent was told or replied. Signed Statements are not an exception to this.</li><li><strong>Isolation of any kind.</strong> A deployment that needs real isolation runs two Shared Agents.</li></ul><h2 id="known-limits" tabindex="-1">Known limits <a class="header-anchor" href="#known-limits" aria-label="Permalink to &quot;Known limits&quot;">​</a></h2><ul><li><strong>One Run at a time, globally.</strong> Throughput is roughly one Run per Run duration for the whole agent. A short question queues behind a long task.</li><li><strong>Nothing is bounded by time.</strong> There is no Run timeout and no Handler timeout. A wedged call halts every party until the Operator restarts the process.</li><li><strong>At-most-once processing.</strong> A failed Signal is dropped after partial effect.</li><li><strong>A withheld Decision is undetectable.</strong> A gap in the sequence means a rolled-back transaction, and the Operator owns the database.</li><li><strong>Swapping the Agent Implementation means rewriting the agent&#39;s configuration.</strong></li></ul><h2 id="where-the-rationale-lives" tabindex="-1">Where the rationale lives <a class="header-anchor" href="#where-the-rationale-lives" aria-label="Permalink to &quot;Where the rationale lives&quot;">​</a></h2><p>Every decision above was recorded when it was made, with the alternatives that were refused and what each choice costs. Those records live in the repository, as numbered decision records under <code>docs/adr/</code>.</p><p>This page states what is true. Those records state why.</p>`,112)])])}const k=s(i,[["render",r]]);export{g as __pageData,k as default};
